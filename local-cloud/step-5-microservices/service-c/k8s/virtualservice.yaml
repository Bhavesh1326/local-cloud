apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: service-c
  namespace: local-cloud-ns
spec:
  hosts:
    - "service-c.local-cloud.com"
  gateways:
    - local-cloud-gateway
  http:
    - route:
        - destination:
            host: service-c
            subset: v1